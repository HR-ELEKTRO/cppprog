% !TEX root = test.tex
% LTeX: language = nl
% !TeX spellcheck = nl_NL
\newcommand{\myClass}{article}
\newcommand{\cursuscode}{\Cpp}
\newcommand{\titel}{Objectgeoriënteerd Programmeren in \Cpp{}}
\newcommand{\documenttype}{Test}
\newcommand{\huidigeversie}{1.0}
\newcommand{\auteurs}{
	J.Z.M. Broeders
}
\include{style_nl_NL}

\usepackage{needspace}

\newcommand{\progs}{progs}
\newcommand{\progsurl}{https://bitbucket.org/HR_ELEKTRO/cppprog/src/master/progs}
\include{styleCode}

% Commando \cpplstinputfragment leest code uit (een deel van) een bestand en plaatst dit in een codefragment.
% #1 = (optional) extra key=value paren voor lstinputlisting b.v.
%      style = linenumbers
%      linerange={1-6, 13-27}
% #2 = filenaam (moet in het directory ./progs staan)
\newcommand{\cpplstinputfragment}[2][]{%
	\lstinputfragment[%
	style=cppstyle,%  
	#1%
	]{#2}%
}

% De code in de environments cpplstfragment wordt in een codefragment geplaatst.
% #1 = (optional) extra key=value paren voor lstset
\lstnewenvironment{cpplstfragment}[1][]{%
	\lstset{
		style=cppstyle,%  
		aboveskip=.6\baselineskip,%
		#1%
	}%
}{%
}

% gebruik voor opmaak van warnings en error meldingen van de C++ compiler
\newcommand{\error}{\texttt}
\usepackage{upgreek}

\begin{document}
\begin{cpplstShortInline}

Hier komt de testcode!

% \needspace start een nieuwe paragraaf
Bla bla bla.
\needspace{3\baselineskip}
Wauwel, wauwel, wauwel.
% Hoe is dat te voorkomen?

% The parse() method must be constexpr and has to return an iterator to either ctx.end() or to the '\}' character.
% * The format() method must be const and return the ctx.out() iterator.
% * While some standard implementations allow you to specify a concrete type for ParseContext and FormatContext, it doesn't work with all. Cppreference uses those templates, and so do I.

% When parsing the format expression, the ctx iterator given to you will point to the first letter after ':' (if there is any), or to '\}', or the iterator will equal to end().



















\end{cpplstShortInline}
\end{document}